{
  "ConnectionStrings": {
    // 02-09-2026: Development apunta al SANDBOX, no a la base real. La API lee lo mismo que
    // publica GestionGD, asi que las dos tienen que mirar la misma base o las pruebas no
    // cierran: se publica en una y se consulta en otra.
    //
    // El sandbox se crea y se refresca con scripts\pruebas\Refrescar-Sandbox.ps1.
    "DefaultConnection": "Server=LP-GD-JAGM\\SQLEXPRESS;Database=DigerTramitesEstado_Unificada_Sandbox;Trusted_Connection=True;TrustServerCertificate=True;"

    // Para apuntar a la base real hay que descomentar a mano, a proposito.
    //"DefaultConnection": "Server=LP-GD-JAGM\\SQLEXPRESS;Database=DigerTramitesEstado_Unificada;Trusted_Connection=True;TrustServerCertificate=True;"
  },
  "PortalDigitalApi": {
    // Clave de desarrollo. No es un secreto: solo abre la API local contra el sandbox.
    // En produccion sigue pendiente decidir donde viven los secretos (P-03).
    "ApiKey": "clave-de-pruebas-local"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}