{
  // La base la manda PortalDigital: acá solo se lee. En producción, con el usuario de solo
  // lectura de scripts/sql/10-usuario-solo-lectura-api.sql.
  "ConnectionStrings": {
    "DefaultConnection": "Server=.;Database=DigerTramitesEstado;Trusted_Connection=True;TrustServerCertificate=True;"
  },
  // Dónde vive SOL. La dirección de cada trámite se COMPONE con esto, así que un host
  // equivocado no rompe un enlace: los rompe todos, y sin dar error.
  // Confirmado por DIGER el 25-08-2026. PortalDigital tiene el mismo valor en su propia
  // configuración: los dos publican enlaces a SOL y tienen que apuntar al mismo sitio.
  "Sol": {
    "UrlBase": "https://sol.pdihonduras.gob.hn"
  },
  "PortalDigitalApi": {
    // La clave real NUNCA va aquí (este archivo se versiona). En desarrollo:
    //   dotnet user-secrets set "PortalDigitalApi:ApiKey" "<clave>" --project src/Api
    // En producción: pendiente de decidir dónde viven los secretos (P-03).
    "ApiKey": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*"
}
