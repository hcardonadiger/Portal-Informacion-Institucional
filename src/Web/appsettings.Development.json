{
  "ConnectionStrings": {
    // 02-09-2026: Development apunta al SANDBOX, no a la base real.
    //
    // El motivo es que aca se prueba de verdad: se entra, se guarda, se publica. Eso deja
    // rastro que no se limpia con un DELETE -bitacora, historial, sellos de UpdatedAt y
    // UpdatedBy en filas que ya existian- y la base unificada todavia tiene que fusionarse
    // con la de TEST. Con esta linea, encender el sistema no puede escribir en ella, venga
    // el arranque de Visual Studio, de dotnet run o de Iniciar-Todo.ps1.
    //
    // El sandbox se crea y se refresca con scripts\pruebas\Refrescar-Sandbox.ps1. Se puede
    // ensuciar sin culpa: se rehace en dos minutos.
    "DefaultConnection": "Server=LP-GD-JAGM\\SQLEXPRESS;Database=DigerTramitesEstado_Unificada_Sandbox;Persist Security Info=True;User ID=sa;Password=admin123;Trust Server Certificate=True;"

    // Para apuntar a la base real hay que descomentar a mano, a proposito: que cueste un
    // gesto consciente es justamente lo que evita el accidente.
    //"DefaultConnection": "Server=LP-GD-JAGM\\SQLEXPRESS;Database=DigerTramitesEstado_Unificada;Persist Security Info=True;User ID=sa;Password=admin123;Trust Server Certificate=True;"

    // Las cadenas que traia dev, conservadas al fusionar y comentadas por lo mismo de arriba:
    // el ambiente de desarrollo no arranca contra nada que no sea el sandbox sin que alguien
    // lo decida a mano.
    //
    // 2026-09-01: GestionGD_TEST, restaurada del respaldo DigerTramitesEstado_v3 (27-08-2026,
    // el segundo juego del .bak, que es el mas reciente) mas los tres guiones de esquema del
    // 31-08. Va en la instancia SQL2025 y no en la que se venia usando: el respaldo se tomo en
    // SQL Server 2025 y SQL Server no restaura hacia atras, asi que en SQLEXPRESS (2022) no entra.
    // Autenticacion de Windows y no usuario/clave: SQL2025 esta en modo Windows-only y 'sa'
    // esta deshabilitado.
    //"DefaultConnection": "Server=localhost\\SQL2025;Database=GestionGD;Trusted_Connection=True;Trust Server Certificate=True;"
    //"DefaultConnection": "Data Source=(localdb)\\MSSQLLocalDB;Initial Catalog=GestionGD;Persist Security Info=True;User ID=sa;Password=admin123;Trust Server Certificate=True;"
  },
  "Supabase": {
    "Url": "https://gnpvqiyantdtksfvqtqk.supabase.co",
    "AnonKey": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImducHZxaXlhbnRkdGtzZnZxdHFrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODA1ODE5NDIsImV4cCI6MjA5NjE1Nzk0Mn0.u7GxYKoQ_G3gokOg2VarZ707GEGc0dnjjYJNl5n5lDw"
  },
  // Credenciales de Supabase: van en User Secrets (UserSecretsId: diger-tramites-estado-web).
  //   dotnet user-secrets set "Supabase:Url"     "https://<proyecto>.supabase.co" --project src/Web
  //   dotnet user-secrets set "Supabase:AnonKey" "<anon-key>"                     --project src/Web

  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
 
